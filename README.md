# Frontend

React frontend for the DACBY Hacker News scraper assignment.

## Run
```bash
npm install
npm run dev
```

## Env
Create `.env` from `.env.example`:
```env
VITE_API_BASE_URL=http://localhost:4000
```

Full documentation is available in the root [`README.md`](../README.md).
